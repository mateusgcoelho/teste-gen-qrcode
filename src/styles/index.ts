import { createStitches } from "@stitches/react";

export const {
  config,
  styled,
  globalCss,
  keyframes,
  getCssText,
  theme,
  createTheme,
  css,
} = createStitches({
  theme: {
    colors: {
      vrSoftware: "#F7A417",
      gray: "#ABABAB",
      lightGray: "#fffbf7",
    },
  },
});
